version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: mysql_horas_extra
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: bijwuurdjvy1uz5oy463
      MYSQL_USER: uep3pwbt2l3sgmiv
      MYSQL_PASSWORD: s0zGJt0zI1akbBtnIXn9
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    command: --default-authentication-plugin=mysql_native_password

volumes:
  mysql_data:
